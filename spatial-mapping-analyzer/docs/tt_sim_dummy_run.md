# Observed real TT-Sim dummy run

Validated locally with the unmodified upstream Wormhole source at
`40bb1a2ad6a755279c4628ddc65e30b10721fdef`, built with g++ 13.3.0 and Python 3.12.
The library SHA-256 from this build was
`2d3604072c303a8a8848ede01fa2283fa2dc5d7ac5af2c8c09a919ae4b2f4837`.
Binary hashes can differ with another toolchain; each invocation records its
actual hash rather than assuming a match to this one.

```bash
python run_analyzer.py --arch examples/wormhole.yaml \
  --program examples/matmul_relu_matmul.yaml --backend tt-sim \
  --iterations 3 --output results/real-tt-sim-e2e
```

Observed output:

```text
Backend: tt-sim; output: results/real-tt-sim-e2e
trial_0000  ok  cost=1 arbitrary (synthetic)
trial_0001  ok  cost=1 arbitrary (synthetic)
trial_0002  ok  cost=1 arbitrary (synthetic)
Best: trial_0000
TT-Sim mode executes a BRISC dummy only; program DAG lowering and hardware latency remain unavailable.
```

Relevant fields from each runner result:

```json
{
  "status": "ok",
  "workload": "wormhole_brisc_add_u32_v1",
  "pci_device_id": "0x401e1e52",
  "core": {"x": 1, "y": 1, "processor": "BRISC"},
  "inputs": {"lhs": 3, "rhs": 3},
  "expected_result": 6,
  "actual_result": 6,
  "completion_flag": 1,
  "dummy_correctness": "passed",
  "program_dag_executed": false,
  "clock_steps_to_completion": 7
}
```

The host wrote only the binary and initial data, verified the pending output
before clocking, and read the result after BRISC execution. The suite also
changed the inputs to 1 and 2 and observed 3, and confirmed an illegal firmware
instruction terminates only the runner subprocess. All 21 tests passed with
the real library present; no integration tests were skipped.

The mapping cost above is deliberately still synthetic. Seven simulator API
steps are not a performance measurement of the input DAG. Full per-trial
artifacts are regenerated by the command and uploaded by the CI workflow as
`tt-sim-dummy-e2e`.
